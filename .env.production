NODE_ENV=production

# Empty string routes all tile API calls through relative paths (/cog/, /stac/, /colorMaps/),
# which the production nginx proxies to planetcantile on the same origin.
VITE_SERVER_URL=
